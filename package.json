{
  "name": "resonance-site-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright smoke tests for the Resonance Ventures landing site",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
